# =============================================================================
# AISkillScore — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in real values.
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# All other variables are server-only.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase
# -----------------------------------------------------------------------------
# Project URL and anon key from: https://supabase.com/dashboard/project/znntwsrwhbvtzbkeydfj/settings/api
# IMPORTANT: The correct project ref is znntwsrwhbvtzbkeydfj — do NOT use eakxgbdouyqybkglowyn (dead project)
NEXT_PUBLIC_SUPABASE_URL=https://znntwsrwhbvtzbkeydfj.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key — NEVER expose to the browser. Used by API routes and Edge Functions.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------------------------------------------------
# Stripe (token packs + lifetime deal)
# -----------------------------------------------------------------------------
# Dashboard: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Webhook signing secret from: https://dashboard.stripe.com/test/webhooks
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs for each token pack (create in Stripe Dashboard → Products)
STRIPE_PRICE_STARTER=price_...
STRIPE_PRICE_PRO=price_...
STRIPE_PRICE_POWER=price_...
STRIPE_PRICE_LIFETIME_EARLY=price_...
STRIPE_PRICE_LIFETIME=price_...
STRIPE_PRICE_LIFETIME_VIP=price_...

# -----------------------------------------------------------------------------
# AI — OpenRouter (powers all 10 text-based AI tools via 3-tier model routing)
# -----------------------------------------------------------------------------
# Get your key at: https://openrouter.ai/settings/keys
# Models used: Gemini 2.5 Flash (premium), DeepSeek V3.2 (standard), GPT-4.1-mini (fallback)
OPENROUTER_API_KEY=sk-or-v1-...

# -----------------------------------------------------------------------------
# Replicate (AI headshots — FLUX 1.1 Pro)
# -----------------------------------------------------------------------------
# Get your API token at: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_...

# -----------------------------------------------------------------------------
# Email — Resend (transactional emails: welcome, daily reminders, results)
# -----------------------------------------------------------------------------
# Get your API key at: https://resend.com/api-keys
# The app no-ops gracefully when this is missing — emails just won't send.
#
# IMPORTANT: The FROM address (noreply@aiskillscore.com) requires domain verification.
# Until your domain is verified in Resend, emails will fail or land in spam.
# During development, use Resend's test mode (emails only send to your own address).
#
# To verify your domain:
#   1. Go to https://resend.com/domains
#   2. Add "aiskillscore.com" (or your domain)
#   3. Add the DNS records Resend provides:
#      - SPF (TXT record)
#      - DKIM (TXT record)
#      - DMARC (TXT record, optional but recommended)
#   4. Wait for verification (usually < 1 hour)
#
# For local development without a verified domain, the app will log
# "[Email] Resend not configured, skipping email: ..." to the console.
RESEND_API_KEY=re_...

# -----------------------------------------------------------------------------
# Cron — Daily Email Reminders
# -----------------------------------------------------------------------------
# Shared secret for authenticating the Vercel Cron job that triggers daily
# reminder emails. Vercel automatically sends this as a Bearer token in the
# Authorization header when calling /api/cron/daily-reminders.
#
# Requirements:
#   - At least 16 characters (Vercel recommendation)
#   - Set in Vercel Dashboard → Settings → Environment Variables
#   - The cron schedule is configured in vercel.json (daily at 10:00 AM UTC)
#
# Generate a strong secret:
#   openssl rand -base64 32
CRON_SECRET=your-cron-secret-at-least-16-chars

# -----------------------------------------------------------------------------
# Analytics — PostHog (optional)
# -----------------------------------------------------------------------------
# Sign up at: https://posthog.com
# Events tracked: tool runs, token purchases, referrals, daily credits, shares
NEXT_PUBLIC_POSTHOG_KEY=phc_...
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# -----------------------------------------------------------------------------
# App Configuration
# -----------------------------------------------------------------------------
# Base URL for email CTAs, OG images, and share links.
# Set to your production URL in Vercel; defaults to https://aiskillscore.com.
NEXT_PUBLIC_APP_URL=http://localhost:3000
